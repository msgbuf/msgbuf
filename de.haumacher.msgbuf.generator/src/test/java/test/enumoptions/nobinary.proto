package test.enumoptions.nobinary;

option NoBinary;

/** Enums honor `option NoBinary` like messages do (issue #20). */
message M {
	enum Nested {
		A;
		B;
	}
	Nested nested;
	TopLevel top;
}

enum TopLevel {
	X;
	Y;
}
